/* these are all applicable header tags, hopefully followed by a little explanation*/

/**
contains funtion prototypes for the C++ standard input/output functions
*/
#include <iostream> 

/**
funtion prototypes for stream manipulators that format streams (deeper look into iostreams)
*/
#include <iomanip>

/**
function prototypes for convresions of numbers to text, text to numbers, 
*/
#include <cmath>

/**
funtions for manipulating the time and date
*/
#include <ctime>

/**
these are all funtions for the standard library containers (containers store data suring program execution
*/
#include <array> 
#include <vector>
#include <list>
#include <forwar_list>
#include <deque>
#include <queue>
#include <stack>
#include <map>
#include <unordered_map>
#include <unordered_set>
#include <set>
#include <bitset>

/** 
function prototypes that test characters for certain properties (if a character is digit or pucntuation) & functions that can be used to switch the case of letters
*/
#include <cctype>

/**
prototypes for C-style string-processing functions
*/
#include <cstring>

/**
classes for runtime type identification
*/
#include <typeinfo>

/**
exception handling
*/
#include <exception>
#include <stdexcept>

/**
C++ stdlib to allocate memory to the standard containers
*/
#include <memory>

/**
input and output to files on disk
*/
#include <memory>

/**
definition of class string
*/
#include <string>

/**
input & output with strings to/from memory (string stream)
*/
#include <sstream>

/**
classes and funtions used by stdlib algorithms
*/
#include <functional>

/**
accessing data in std lib containers
*/
#include <iterator>

/**
funtions for manipulating data
*/
#include <algorithm>

/**
macros for adding diagnostics to aid debugging
*/
#include <cassert>

/**
floating point size limits of the system
*/
#include <cfloat>

/**
integral size limits of the system
*/
#include <climits>

/**
prototypes for the C-style stdin/stdout 
*/
#include <cstdio>

/**
classes and functions used by stream processing to process data in the natural format for different languages (monetary formats, sorting strings)
*/
#include <locale>

/**
classes for defining the numerical data type limits on each platform
*/
#include <limits>

/** 
contains classes & functions that are used by many stdlib headers
*/

#include <utility>
